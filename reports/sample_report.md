# Dataset Explorer Agent - Automated Report
Generated by the **Dataset Explorer Agent** (Offline Analysis Pipeline).

## 1. Dataset Overview
- **Total Rows**: 154
- **Total Columns**: 6
- **Columns List**:
  - `passenger_id`
  - `age`
  - `salary`
  - `gender`
  - `city`
  - `purchased`

### First 5 Rows Preview:
| passenger_id   |   age | salary   | gender   | city     |   purchased |
|:---------------|------:|:---------|:---------|:---------|------------:|
| ID_001         |  41   | $63,000  | Other    | New York |           1 |
| ID_002         |  33.3 | $93,000  | Female   | New York |           0 |
| ID_003         |  42.8 | $72,000  | Female   | New York |           0 |
| ID_004         |  53.3 | $63,000  | Male     | New York |           0 |
| ID_005         |  32.2 | $114,000 | Male     | New York |           0 |


## 2. Data Quality Analysis
- **Overall Quality Score**: 77.9/100 (Good)
- **Total Duplicate Rows**: 4 (2.6%)
- **Total Missing Cells**: 20 (2.16%)

### Missing Values & Data Types by Column:
| Column         | Data Type   |   Missing Count | Missing Percentage (%)   |
|:---------------|:------------|----------------:|:-------------------------|
| `passenger_id` | object      |               0 | 0.00%                    |
| `age`          | float64     |              12 | 7.79%                    |
| `salary`       | object      |               0 | 0.00%                    |
| `gender`       | object      |               8 | 5.19%                    |
| `city`         | object      |               0 | 0.00%                    |
| `purchased`    | int64       |               0 | 0.00%                    |


### Detected Data Type Inconsistencies:
- **Column `salary`**: Currently `object`, suggested `numeric`.
  - *Reason*: Over 80% of sampled values (100/100) can be parsed as numbers after stripping symbols like $, %.


## 3. Outlier Analysis Summary
- **Total IQR Outliers Detected**: 4
- **Total Z-Score (|Z| > 3) Outliers Detected**: 2
- **Number of Numeric Columns Analyzed**: 2

### Outliers per Numeric Column:
| Column      |   Mean |   Std Dev |   IQR Outlier Count | IQR Boundaries   |   Z-Score Outlier Count |
|:------------|-------:|----------:|--------------------:|:-----------------|------------------------:|
| `age`       | 34.42  |    14.766 |                   4 | [5.525, 61.325]  |                       2 |
| `purchased` |  0.292 |     0.456 |                   0 | [-1.5, 2.5]      |                       0 |


## 4. Preprocessing Recommendations
### 4.1 Remove duplicates (✅ Recommended)
- **Scope**: Global Rows
- **Justification**: The dataset contains 4 duplicate rows. Removing duplicate rows prevents training models on repeated observations, which can cause overfitting, and ensures honest performance evaluations.

### 4.2 Drop constant columns (✅ Recommended)
- **Scope**: Columns: 'city'
- **Justification**: The column(s) 'city' contain only one unique value (excluding missing values). Constant columns carry zero variance and zero predictive power. Dropping them saves memory and prevents collinearity issues.

### 4.3 Fill missing values using Median (✅ Recommended)
- **Scope**: Numeric Columns: age
- **Justification**: Missing values detected in numeric columns: 'age' (12 missing). Imputing missing values with the median is robust to outliers, protecting the central tendency of the feature without biasing models with artificial extreme values.

### 4.4 Fill missing values using Mode (✅ Recommended)
- **Scope**: Categorical Columns: gender
- **Justification**: Missing values detected in categorical columns: 'gender' (8 missing). Imputing missing values with the mode (most frequent category) is standard practice for categorical data since mathematical averages (mean, median) cannot be calculated.

### 4.5 Label encode categorical columns (✅ Recommended)
- **Scope**: Categorical Columns: 'passenger_id', 'salary', 'gender', 'city'
- **Justification**: Categorical columns detected: 'passenger_id', 'salary', 'gender', 'city'. Scikit-learn machine learning algorithms require numerical input matrices. Label encoding (or mapping category strings to numeric integers) converts text categories into a form the models can process.

### 4.6 StandardScaler scaling (✅ Recommended)
- **Scope**: Numeric Features: 'age', 'purchased'
- **Justification**: Numeric columns exist in the dataset (Note: Feature scales differ drastically, with standard deviations ranging from 0.46 to 14.77).. Standardizing numeric columns to unit variance and zero mean is essential for scale-sensitive algorithms (like Logistic Regression, SVM, and k-NN) to prevent larger scale values from disproportionately dominating optimization and distances.

## 5. Machine Learning Model Recommendations
- **Target Column**: `purchased`
- **ML Problem Type Detected**: **Classification**

Based on target column 'purchased' (type: int64, unique values: 2), the task was classified as a **Classification** problem. Below are the recommended models:

### Model: Logistic Regression
- **Pros**: Highly interpretable, fast training and inference, outputs probability estimates, easily regularized (L1/L2).
- **Cons**: Assumes linear boundary; struggles with complex non-linear interactions without manual feature engineering.
- **Why Recommended**: Excellent baseline. Use this to determine linear separability of classes and when decision transparency is required.

### Model: Random Forest Classifier
- **Pros**: Strong out-of-the-box accuracy, handles non-linear relationships, robust to outliers and noise, provides feature importances.
- **Cons**: Can be memory-heavy, slow to predict on massive datasets, functions as a 'black-box' ensemble.
- **Why Recommended**: Usually the top-performing non-linear baseline. Very robust for tabular datasets, requiring minimal hyperparameter tuning.

### Model: Decision Tree Classifier
- **Pros**: Extremely interpretable (visualizable structure), handles mixed data types natively, requires zero feature scaling.
- **Cons**: Highly prone to overfitting (high variance) if tree depth is not restricted, highly sensitive to training data perturbations.
- **Why Recommended**: Good for generating simple, clear decision rules and understanding hierarchical feature relationships.

### Model: Support Vector Machine (SVM)
- **Pros**: Effective in high-dimensional spaces, versatile kernel tricks (RBF, Polynomial) allow modeling of complex boundaries, robust against overfitting.
- **Cons**: Requires feature scaling (e.g. StandardScaler), computationally expensive for large datasets (O(n_samples^2 * n_features)), lacks direct probability mapping.
- **Why Recommended**: Strong choice if classes are separated by a complex non-linear boundary, especially in medium-sized datasets. Must scale features first.

## 6. Conclusion and Next Steps
Based on the data profiling, here is the suggested pipeline pathway:
1. **Data Cleaning**: Execute rows deduplication and impute the missing values in numeric/categorical variables using the recommended methods.
2. **Feature Engineering**: Standardize features using standard scaling if you plan to train linear models or support vector machines, and encode categorical variables using label or one-hot encoding.
3. **Cross-Validation Setup**: Partition the data using standard cross-validation strategies (Stratified K-Fold for Classification; K-Fold for Regression) before fitting models.
4. **Baseline Modeling**: Train the recommended models and compare their metrics (accuracy, F1-score, ROC-AUC for classification; RMSE, MAE, R² for regression).